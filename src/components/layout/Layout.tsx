import { Navbar } from "../navbar/Navbar";
import { Home } from "../views/Home";

export const Layout = () => {
    return (
        <div>
            <Navbar />
            <Home />
        </div>
    );
};
